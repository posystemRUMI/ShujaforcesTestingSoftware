# ==============================================================================
# Forces Academy Computerized Testing Software — Environment Configuration
# ==============================================================================

# Client-Side Supabase Configuration (Public Anonymous Access)
# These variables are exposed to Vite client bundle via `import.meta.env`
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key-here

# Optional: Set to 'mock' for standalone offline UI preview only (default: production Supabase)
# VITE_DATA_MODE=production

# ------------------------------------------------------------------------------
# SERVER-ONLY CREDENTIALS (DO NOT EXPOSE TO FRONTEND BUNDLE)
# NEVER prefix with VITE_. Restricted exclusively to trusted migration scripts,
# CI/CD deployment pipelines, or secure backend services.
# ------------------------------------------------------------------------------
# SUPABASE_SERVICE_ROLE_KEY=your-super-secret-service-role-key-here
# DATABASE_URL=postgresql://postgres:your-db-password@db.your-project-ref.supabase.co:5432/postgres
